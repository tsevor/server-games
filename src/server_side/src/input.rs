//we are going to use arc of a map to handle inputs 

#[derive(Default, Clone)]
pub struct Keys{
    pub key_a: bool,
    pub key_b: bool,
    pub key_c: bool,
    pub key_d: bool,
    pub key_e: bool,
    pub key_f: bool,
    pub key_g: bool,
    pub key_h: bool,
    pub key_i: bool,
    pub key_j: bool,
    pub key_k: bool,
    pub key_l: bool,
    pub key_m: bool,
    pub key_n: bool,
    pub key_o: bool,
    pub key_p: bool,
    pub key_q: bool,
    pub key_r: bool,
    pub key_s: bool,
    pub key_t: bool,
    pub key_u: bool,
    pub key_v: bool,
    pub key_w: bool,
    pub key_x: bool,
    pub key_y: bool,
    pub key_z: bool,
    pub key_space: bool,
    pub key_enter: bool,
    pub key_shift: bool,
    pub key_ctrl: bool,

}

