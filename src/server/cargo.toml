[package]
name = "server"
version = "0.0.1"
edition = "2026"
authors = ["catalyste idk"]
description = "Server crate for server-games"
license = "MIT"
publish = false

[dependencies]

# Add runtime or utility crates here as needed, e.g.:
# tokio = { version = "1", features = ["full"] }
# anyhow = "1"
